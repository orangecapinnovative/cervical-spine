var Broker = function() {
  console.warn('This is cervical-spine library and broker should not be run')
}

Broker.prototype.start = function(cb) {
  console.warn('This is cervical-spine library and broker should not be run')
  cb && cb()
}

Broker.prototype.stop = function(cb) {
  console.warn('This is cervical-spine library and broker should not be run')
  cb && cb()
}

module.exports = Broker